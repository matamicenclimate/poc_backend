{
  "routes": [
    {
      "method": "GET",
      "path": "/app-config",
      "handler": "app-config.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/app-config",
      "handler": "app-config.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/app-config",
      "handler": "app-config.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
