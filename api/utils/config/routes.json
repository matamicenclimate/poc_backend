{
  "routes": [
    {
      "method": "GET",
      "path": "/chart/balance/me",
      "handler": "utils.chartBalanceMe",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contracts/info",
      "handler": "utils.contractsInfo",
      "config": {
        "policies": []
      }
    }
  ]
}
